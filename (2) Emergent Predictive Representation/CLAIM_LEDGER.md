# Claim ledger

## Proved under stated assumptions

- A normalized pointer with a nontrivial irreducible stationary real one-parameter action has a two-dimensional cyclic span.
- The connected **unnormalized** Lorentz-cone filter automorphisms preserving both QND rays have Lie algebra `R K ⊕ so(d)`, modulo global dilation.
- If every vector in the reference span is independently accessible and maps injectively and surjectively onto that filter algebra, then `d = 2`; within irreducible rank-`≥2` finite-dimensional EJAs this selects the complex Hermitian family.
- The redundant pure-spinor map `Q(ψ) = ψψ†` has a `U(1)` fibre. The EJA theorem does not make points on that fibre physical.
- The QND transverse coordinate `cφ` is recoverable from `ρ` and cannot provide a same-`ρ` Court-5 pair.
- If all adaptive controlled process laws factor through `Q` on a matched standard-process slice, every protocol-level fibre variation is zero.

## Computed

- Supplied symbolic QND invariant and quotient have zero Itô drift and diffusion residuals.
- Supplied control search recovers `|nz| = 1/√3` and full local observability.
- Born, unitary, dephasing, Rabi, Zeno, singlet, CHSH, and no-signalling baseline checks pass.
- Inserting opposite global phases leaves `ψψ†` unchanged; the code does not simulate closed controls, reset, or a memory carrier.
- An ordinary quantum cavity can retain `|+α⟩` versus `|-α⟩`: equal photon number and equal reduced qubit state coexist with nonzero homodyne total variation.
- In the short Gaussian VD-Hopf-1 ansatz with fixed protocol `p` and matched process state `η0`,

  \[
  V_{\rm p}(\rho;\eta_0)
  \simeq2\Phi(a|\varepsilon|(1-z^2)\sqrt T)-1,
  \qquad
  V^\star_{{\rm p},\eta_0}
  \simeq2\Phi(a|\varepsilon|\sqrt T)-1.
  \]

## Candidate, not established

- A gauge-invariant closed-history memory `Γrel` remains physically instantiated after the ray returns.
- A nominally QND pure-state phenomenological record contains

  \[
  \varepsilon(1-z^2)\sin\Gamma_{\rm rel}.
  \]

- Nature has `ε ≠ 0`.

## Open

- Derive detector-response completeness from weaker physical coupling principles.
- Derive an equivariant/isometric response that identifies detector rotation with system complex structure; dimension matching alone does not do so.
- Complete the literature novelty audit for the exact conditional scalar characterization.
- Derive the Hopf-memory coupling and its coefficient from a reversible microscopic `Ψ`.
- Define a mixed-state, completely positive, composable instrument and explicit memory carrier.
- Construct a full bipartite, no-signalling, Bell-consistent extension.
- Implement and beat frozen bounded qutrit–cavity and process-tensor models on externally committed data.
- Independently replicate any nonzero result.

## Outcome ladder

- **A:** conditional quantum representation characterized; explanatory reconstruction.
- **B:** `Q` many-to-one; representational redundancy only.
- **C:** matched protocol-bounded `V* > 0`; the declared reduced state/process model is predictively incomplete.
- **D:** C fails but accepted dynamics of a sufficient `ρ` fail; modified quantum dynamics.
- **E:** C and D fail in the accessible domain; quantum theory remains predictive and Voss Dynamics is explanatory/methodological.
